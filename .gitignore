# Local secrets — never commit (see SOPS+age for in-repo encrypted config)
.env
.env.*
*.key
*.pem
.secrets/

# direnv
.direnv/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Go build output
/bin/
/dist/
/controller
coverprofile

# Development environment — dev-kit downloads/generates these per-machine
common.mk
.common.mk-checked
.common.mk-version
.common.mk-download
/.pre-commit-config.yaml
/.devenv/
/result
*.tgz