---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: consumptionquotas.quota.opendefense.cloud
spec:
  group: quota.opendefense.cloud
  names:
    kind: ConsumptionQuota
    listKind: ConsumptionQuotaList
    plural: consumptionquotas
    singular: consumptionquota
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ConsumptionQuota caps how many instances of a governed APIExport resource each
          consumer workspace may create. Created by a provider alongside its APIExport.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              autoApproveCeiling:
                description: AutoApproveCeiling is reserved for Phase 2 (self-service)
                  and ignored in Phase 1.
                format: int32
                type: integer
              by:
                default: Count
                description: By selects the accounting mode. Phase 1 supports only
                  "Count".
                enum:
                - Count
                type: string
              defaultLimit:
                description: DefaultLimit applies to every consumer workspace with
                  no grant.
                format: int32
                minimum: 0
                type: integer
              governed:
                description: Governed is the exported resource type this quota caps.
                properties:
                  apiExportName:
                    description: APIExportName is the APIExport in the same workspace
                      as this policy.
                    minLength: 1
                    type: string
                  group:
                    minLength: 1
                    type: string
                  resource:
                    minLength: 1
                    type: string
                  version:
                    minLength: 1
                    type: string
                required:
                - apiExportName
                - group
                - resource
                - version
                type: object
            required:
            - by
            - defaultLimit
            - governed
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              identityHash:
                description: |-
                  IdentityHash is resolved by the controller from the governed APIExport's
                  status.identityHash. It disambiguates same-named resources from different providers.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
