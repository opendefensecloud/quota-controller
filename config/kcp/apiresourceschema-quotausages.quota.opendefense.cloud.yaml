# QuotaUsage is an INTERNAL resource — it is applied as a plain CRD in the
# quota-ctrl workspace (config/crds/quota.opendefense.cloud_quotausages.yaml).
# This APIResourceSchema is generated for completeness / future use but it is
# NOT referenced by the quota-provider APIExport's latestResourceSchemas.
apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  name: v260702-d8e4b15.quotausages.quota.opendefense.cloud
spec:
  group: quota.opendefense.cloud
  names:
    kind: QuotaUsage
    listKind: QuotaUsageList
    plural: quotausages
    singular: quotausage
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      description: |-
        QuotaUsage is the internal, authoritative accounting object for one
        (consumerCluster, group, resource, identityHash). Not exported to providers or consumers.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: |-
            QuotaUsageSpec records the identity this ledger belongs to (for humans/debugging;
            enforcement keys by object name). All fields are set once at creation.
          properties:
            consumer:
              type: string
            group:
              type: string
            identityHash:
              type: string
            resource:
              type: string
          required:
          - consumer
          - group
          - identityHash
          - resource
          type: object
          x-kubernetes-validations:
          - message: spec is immutable
            rule: self == oldSelf
        status:
          properties:
            confirmed:
              description: Confirmed is the real live object count, owned by the
                accounting reconciler.
              format: int32
              minimum: 0
              type: integer
            reservations:
              description: Reservations are admits allowed but not yet observed
                as live objects.
              items:
                description: Reservation is an in-flight admit slot with a TTL.
                properties:
                  expiresAt:
                    description: ExpiresAt is when the sweep may reclaim this reservation
                      if unfulfilled.
                    format: date-time
                    type: string
                  key:
                    description: |-
                      Key identifies the reserved object: "namespace/name" for namespaced
                      objects, the bare "name" for cluster-scoped ones (quota.ObjectKey), or
                      "uid:<request UID>" for generateName requests whose final name is not
                      known at admission time.
                    type: string
                required:
                - expiresAt
                - key
                type: object
              type: array
          required:
          - confirmed
          type: object
      required:
      - spec
      type: object
    served: true
    storage: true
    subresources:
      status: {}
